<Window x:Class="MyVocabulary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:MyVocabulary.Controls"
        Title="MainWindow"
        Width="819"
        Height="442"
        Closing="Window_Closing"
        Icon="/MyVocabulary;component/Images/MainApp.ico"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl x:Name="TabControlMain">
            <TabItem x:Name="TabItemBadKnown" Header="Bad Known" />
            <TabItem x:Name="TabItemKnown" Header="Known" />
            <TabItem x:Name="TabItemUnknown" Header="Unknown" />
        </TabControl>

        <DockPanel Grid.Row="1">
            <Button x:Name="ButtonImport"
                    Width="90"
                    Height="25"
                    Margin="3"
                    Content="Import" Click="ButtonImport_Click" />
            <Button x:Name="ButtonSave"
                    Width="90"
                    Height="25"
                    Margin="3"
                    HorizontalAlignment="Right" DockPanel.Dock="Right"
                    Click="ButtonSave_Click"
                    Content="Save" />
            <Button x:Name="ButtonOpen"
                    Width="90"
                    Height="25"
                    Margin="3"
                    HorizontalAlignment="Right"
                    Click="ButtonOpen_Click"
                    Content="Open" />
        </DockPanel>
    </Grid>
</Window>
